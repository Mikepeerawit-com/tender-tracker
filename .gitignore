# dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# next.js
.next/
out/
build/
next-env.d.ts

# production
dist/

# environment — never commit WeCom Corp ID / Secret / Agent ID
.env
.env*.local
.env.production

# supabase
.branches
.temp
supabase/.temp/

# vercel
.vercel

# testing
coverage/
# what vitest saves when a browser assertion fails, next to the test that failed
__screenshots__/
# the contact sheet (#78): every screen photographed for somebody to look at, drawn by
# whichever machine ran it, so never a baseline and never committed
.contact-sheet/
.vitest-attachments/

# typescript
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editors
.idea/
.vscode/*
!.vscode/extensions.json

# macos
.DS_Store

# Wizard scratch: captured secrets and hand-check results. Never committed.
.wizard/
